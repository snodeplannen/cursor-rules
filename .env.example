# Configuratie voor MCP Invoice Processor
# Kopieer dit bestand naar .env en pas de waarden aan

# Logging niveau (DEBUG, INFO, WARNING, ERROR)
# Configuratie voor MCP Invoice Processor
# Kopieer dit bestand naar .env en pas de waarden aan

# Logging niveau (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Ollama configuratie
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3
OLLAMA_TIMEOUT=120

# Optioneel: API sleutel voor externe services
# API_KEY=your_api_key_here

# Optioneel: Database configuratie (indien nodig)
# DATABASE_URL=sqlite:///./app.db

# Optioneel: Redis configuratie voor caching
# REDIS_URL=redis://localhost:6379

# Optioneel: Beveiliging
# SECRET_KEY=your_secret_key_here
# ALLOWED_HOSTS=localhost,127.0.0.1LOG_LEVEL=INFO
